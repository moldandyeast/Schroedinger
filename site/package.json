{
  "name": "schroedinger-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "serve": "npx serve public -l 8080"
  },
  "devDependencies": {
    "marked": "^15.0.0"
  }
}
